<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.autorental">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoRental">

        <!-- Main Activity (Home Activity) -->
        <activity android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CategoryDetails Activity -->
        <activity android:name=".CategoryDetails"
            android:exported="true">
        </activity>

        <!-- BrandCarsActivity in the 'logo' package -->
        <activity android:name="com.example.autorental.logo.BrandCarsActivity"
            android:exported="true">
        </activity>

        <!-- Additional activities can be declared here -->

    </application>
</manifest>
